os: osx
osx_image: xcode9.4

# blocklist
branches:
  except:
  - bakcport-0.8.1

before_install:
# install dependancies
  - brew install freetype gettext lua imlib2
  - brew link gettext --force

# setup xcode project
  - cd ConkyX/conky-for-macOS/forConkyX
  - cmake .. -G Xcode
  - cd ../../..

install:
  - xcodebuild -workspace Manage\ Conky.xcworkspace -scheme Manage\ Conky